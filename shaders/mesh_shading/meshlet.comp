#version 450

#include "mesh_shading/mesh.h"

layout(set = 0, binding = 0) readonly buffer Meshlets
{
    Meshlet meshlets[];
};

layout(set = 0, binding = 2) buffer VisibleMeshInstances
{
    MeshDrawCommand mesh_draw_commands[];
}



layout (local_size_x = 32, local_size_y = 1, local_size_z = 1) in;

void main()
{

}