#include "render_pass.h"

namespace xihe::backend
{
RenderPass::RenderPass(Device &device, const std::vector<rendering::Attachment> &attachments, const std::vector<LoadStoreInfo> &load_store_infos, const std::vector<SubpassInfo> &subpasses)
{
 //todo
}
}
